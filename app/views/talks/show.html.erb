
<h1> <%= @talk.film %> </h1>
<h1> <%= @talk.theme %> </h1>
<h1> <%= @talk.date %> </h1>
  <%= link_to "Book here", booking_path(@talk),
          method: :patch  %>


  <% if @talk.user_id == current_user.id  %>

  <%= link_to "Supprimer", talk_path(@talk),
          method: :delete,
          data: { confirm: "Are you sure?" }  %>
  <%= link_to "Modifier", talk_path(@talk),
  method: :patch, data: { confirm: "Are you sure?" }  %>
  <% end %>

